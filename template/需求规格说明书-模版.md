# 文档说明

> **文档类型**：需求规格说明书（SRS），即本仓库流程中的 **迭代级 PRD（交付研发与测试）**。经 **产品经理审核定稿** 后，供 **研发**（方案与实现）、**测试**（验收、用例与回归口径）、**AI**（spec-coding / 辅助实现）使用。  
> **交付物文件名**：`ATO_V{X.Y.Z}-{一级需求描述}-需求规格说明书.md`。`{一级需求描述}` 与迭代需求清单「一级需求」列一致；文件名非法字符（如 `\ / : * ? " < > |`）替换为 `_`。黄金范例见 `template/ATO_*-需求规格说明书-范例-*.md`（含 `-范例-` 后缀；正式稿不含）。

> **《页面需求与交互规格》的定位**（路径：`docs/prd/V{X.Y.Z}/ATO_V{X.Y.Z}-页面需求与交互规格.md`）：用于 **HTML 页面原型 / 线框落地**（布局、控件、§ 章节锚点）；**不是**迭代产品的唯一真源。可与本正文 **§ / REQ** 交叉对照；**研发、测试与实现验收口径以本 SRS 为准**。

> **本文档的定位**：承载 **单个一级需求** 的完整条文（见 **附录 E**）；章节结构、编号层级与写法深度参照黄金范例 **`template/ATO_V1.0.1-P5-需求规格说明书-范例-市场缺陷分析.md`**。**文档粒度（必读）**：**一份 SRS 仅覆盖迭代清单中的一个一级需求**；同一迭代多个一级需求须拆成多份文件。详见 **附录 E**。

**章节引用约定**：正文中的 `## 1.1`、`§1.1.x` **仅在本文档内有效**；跨文档沟通请使用 **文件名 + REQ-ID** 或 **文件名 + §**（示例：`ATO_V{X.Y.Z}-{一级需求描述}-需求规格说明书.md` + **REQ-xxx**），避免口头简称「看 1.1」在多份 SRS 并存时混淆。

**编号约定**（正文 `##` / `###` / `####` 与清单列对应）：

- `## 1.1` — 对应迭代清单 **一级需求**（本文件仅承载 **一个** 一级需求，见附录 E）
- `### 1.1.x` — 对应迭代清单 **二级需求**（能力簇或子模块）
- `#### 1.1.x.y` — 对应迭代清单 **三级需求**（子场景、关键步骤等；无三级时本节可省略）

**页面聚合（可选）**：若清单层级与章节需特殊编排（例如同一 Tab 内多条 REQ 用多个 `####` 聚合），须在本节或对应 `###` / `####` 下 **显式说明** 与 REQ 的映射，避免层级歧义。

**需求描述结构（固定小节，勿删节）**：各功能点「• **需求描述：**」下须 **依次** 包含 **`1. **正常流程**` → `2. **异常场景**` → `3. **限制条件`**。若当前能力无独立异常项，**仍须保留 `2. **异常场景**` 标题**，正文可 **留空**（不得整节删除）。更完整的衍生写法（子功能分述、前置条件与异常分工等）见黄金范例文末 **附录 D**。

**验收基线**（版本通配）

- **迭代级权威（研发 / 测试 / AI）**：**当前迭代已定稿的本 SRS**。业务口径、验收勾选项、与 REQ-ID 对应的条文 **以实现与测试对本 SRS 为准**。
- **REQ 覆盖（完整性）**：本稿所对应 **一级需求** 在迭代清单中的 **全部 REQ**，须在本 SRS 内有 **可定位条文**，或 **显式说明**（不适用、推迟至某迭代、合并至其他 REQ 等）并 **可追溯**；定稿前禁止清单内 REQ 无交代。
- **页面交互规格（原型文档）**：路径形态 **`docs/prd/V{X.Y.Z}/ATO_V{X.Y.Z}-页面需求与交互规格.md`**。**凡本迭代已走 HTML 原型路径且已定稿页面交互规格者**，编写或修订本 SRS 时 **须对照**（§ 锚点、布局与控件形态）；**不得以「可选文档」为由跳过**。**不自动凌驾于**已定稿的需求规格说明书。无原型阶段，或未产出 / 未定稿该文档的迭代可缺省。
- **裁决规则**：当 **页面交互规格**、**原型代码**、**迭代清单** 与本 SRS **冲突**时：**默认以已定稿的需求规格说明书（迭代 PRD）为准**，并 **回写**相关文档或升版 SRS（按变更流程）；若团队临时决议以原型为准，须在 SRS 或变更记录中 **显式记载**。正文引用页面文档 § 仅作 **对照索引**，不替代 SRS 条文。
- **外部系统 / 接口边界**：以前端 **可观测行为**（页面交互、提示、禁用态、列表与报表展示）与本文业务口径为准；**数据源字段语义、同步与过滤口径、接口契约**（如外部系统、RDMS、后台 API）以 **接口文档、数据字典或后台权威定义** 为准。若本 SRS 与后台可实现性冲突，按变更流程 **修订 SRS 或调整接口**；**禁止**在实现中 **silent drift**（未留痕偏离条文）。

**文档生成依据**（编写或机器生成时应 **同时** 打开并对照以下材料）

1. **迭代需求清单**：`docs/prd/V{X.Y.Z}/ATO_V{X.Y.Z}迭代需求清单.md`（文件名以仓库约定为准；重点为一级→二级→三级列与「需求说明 / 需求动机」）。
2. **迭代页面需求与交互规格**（按流程取舍）：同版本目录下的 **`ATO_V{X.Y.Z}-页面需求与交互规格.md`** — 与上文 **验收基线** 中「页面交互规格」**同口径**：**已定稿** 则编写 / 修订本 SRS 时 **须对照**（§ 与控件形态）；**未走 HTML 原型路径** 或 **未产出 / 未定稿** 该文档的迭代可缺省。可将 § 写入 SRS 作索引，**条文与验收以 SRS 为准**。
3. **原型页面与代码逻辑**：对应该迭代的前端实现（典型路径：`src/pages/`、`src/components/`、`src/utils/`、`src/constants/`、`src/types/`、`src/mocks/`、`src/router/` 等），用于核对 **可观测验收点**；代码与 SRS 不一致时按缺陷/变更流程处理。

**本稿绑定（交付前填写）**：版本 **[V{X.Y.Z}]**；一级需求 **[与清单「一级需求」列一致]**；REQ 范围 **[REQ-xxx～yyy 或逐条列出]**；建议路径 **`docs/prd/V{X.Y.Z}/ATO_V{X.Y.Z}-{一级需求描述}-需求规格说明书.md`**；页面交互规格（如有）：`…`；迭代清单：`…`；主要页面 / 组件（可选）：`…`。

---

# 1 需求说明

## 1.1 [一级需求名称]

与迭代需求清单 **一级需求** 列名称一致；正文 REQ 须与本一级需求下清单行 **可追溯**。

### 模块概述

[描述该功能模块的整体业务价值、解决的问题与目标用户群体]

**解决的问题**：

**目标用户**：

**入口**：

[菜单、路由、跳转关系；可从 `docs/spec/01-信息架构与路由.md` 或页面交互规格摘录]

**模块业务逻辑说明**：

[可用文字或 mermaid 流程图描述主链路；涉及外部系统处注明数据权威来源]

---

### 1.1.1 [二级需求名称]

[简述本二级需求覆盖的能力范围；注明对应清单 **REQ-xxx**]

#### 1.1.1.1 [功能点或三级需求标题]（REQ-xxx）

• **背景与动机：**

• **用户场景：**

• **优先级：** [高 / 中 / 低]

• **输入/前置条件：**

• **需求描述：**

1. **正常流程**

2. **异常场景**

   [若无独立异常项，**保留本小节**；正文可留空或写「无」。**勿删除「异常场景」整节。**]

3. **限制条件**

• **补充说明：**

• **验收标准：**

- [ ]

---

> **续写**：在同一 `### 1.1.1` 下按需增加 `#### 1.1.1.2`、`#### 1.1.1.3`…；更多 **二级需求** 使用 `### 1.1.2`、`### 1.1.3`…，编号与迭代清单 **二级 / 三级需求** 对齐。范例见 `template/ATO_V1.0.1-P5-需求规格说明书-范例-市场缺陷分析.md` 正文。

---

# 附录

## 变更历史

> **说明**：本文档可采用「全量文档 + 版本标记」策略；每次更新在表顶追加记录。

| 版本     | 日期         | 变更类型 | 变更内容     | 变更原因 | 影响范围 |
| -------- | ------------ | -------- | ------------ | -------- | -------- |
| V{X.Y.Z} | 2026-05-13 | 规范 | 章节引用示例；页面交互规格强约束；外部系统边界与禁止 silent drift；文档生成依据与验收基线同口径；附录 D 指引含范例 D.6 | 与黄金范例对齐 | 文档说明、验收基线、附录 D 指引 |
| V{X.Y.Z} | YYYY-MM-DD   | 新增     | 初稿         | -        | -        |

**变更类型说明**：新增 / 优化 / 修复 / 删除 / 重构 / 规范。

---

## 附录A：用户角色说明

| 角色名称 | 角色描述 | 主要权限 | 使用场景 |
| -------- | -------- | -------- | -------- |
| [角色1]  | [描述]   | [权限]   | [场景]   |

## 附录B：术语表

| 术语   | 定义 | 业务说明 |
| ------ | ---- | -------- |
| [术语1] | [定义] | [含义] |

## 附录C：参考文档

- **通配路径（任意迭代）**  
  - 页面需求与交互规格：`docs/prd/V{X.Y.Z}/ATO_V{X.Y.Z}-页面需求与交互规格.md`  
  - 迭代需求清单：`docs/prd/V{X.Y.Z}/ATO_V{X.Y.Z}迭代需求清单.md`  
- **（可选）** 原始页面需求设计：`prompt/page_design/AutoTestOne-V{X.Y.Z} 原始页面需求设计.md`（文件名以实际为准）  
- **SRS 结构黄金范例（含附录 D 衍生写法）**：`template/ATO_V1.0.1-P5-需求规格说明书-范例-市场缺陷分析.md`  
- **使用指导**：`使用指导.md`  
- **业务模块拆分（合入全量库）**：`.cursor/rules/requirements-module-split.mdc`

## 附录D：编写规范（引用黄金范例）

三级需求下 **子功能分述**、**前置条件与异常场景分工**、**算法与验收捆绑**、**章节编号与清单层级** 等细则，见黄金范例 **`template/ATO_V1.0.1-P5-需求规格说明书-范例-市场缺陷分析.md`** 文末 **附录 D**（含 **D.6** 跨文档引用、页面 PRD 输入与外部系统边界）。编写提示词或 Cursor 规则时宜直接引用该附录条目。**文首「章节引用约定」与「验收基线」（含页面交互规格强约束、外部系统 / 接口边界）** 与黄金范例保持同版口径。

## 附录E：文档粒度规范（暂行）

> **适用范围**：本仓库《需求规格说明书》（迭代 PRD）的编制与评审；与交付文件名 `ATO_V{X.Y.Z}-{一级需求描述}-需求规格说明书.md`（见 `使用指导.md`）一致。

### E.1 一份 SRS 仅允许对应一个一级需求

- **严格要求**：**一份**需求规格说明书（迭代 PRD）**只能覆盖迭代需求清单中的某一个一级需求**，及其下属的二级、三级需求。**禁止**在同一份 SRS 正文中并列承载 **多个不同一级需求** 的产品条文。
- **迭代内多个一级需求**：须 **拆成多份 SRS 文件**，每份独立命名、独立定稿；文件名中 `{一级需求描述}` 与该份文档唯一对应的一级需求一致（与清单「一级需求」列对齐；非法路径字符替换为 `_`）。
- **本稿**：`## 1.1 [一级需求名称]` 仅描述 **唯一** 一级需求；REQ 范围仅涵盖该一级需求在清单中的条目。

### E.2 与全量需求库拆分的衔接

- 合入 `docs/requirements/` 时仍遵循 `.cursor/rules/requirements-module-split.mdc` 等业务域规则；**SRS 按一级需求拆分** 与 **全量库按业务域归档** 的对应关系，以评审通过的映射为准。

---

**文档版本：** V{X.Y.Z}  
**最后更新：** 2026-05-13（与黄金范例文首：章节引用、页面 PRD 强约束、外部系统边界对齐；交付定稿后请改为实际日期）  
**编写人：** [产品经理 / AI]  
**审核人：** [业务方 / 测试负责人]
